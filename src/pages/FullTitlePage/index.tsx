import React from 'react';

import { FullTitleItem } from '@/features/FullTitleItem';

type Props = {};
const FullTitlePage: React.FC<Props> = (props) => {
    const {} = props;

    return <FullTitleItem />;
};

export default FullTitlePage;
